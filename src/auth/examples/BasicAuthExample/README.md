# BasicAuth Example

This example demonstrates basic username/password authentication using Ivy's built-in `BasicAuthProvider`.

The values in `appsettings.json` provide authentication for a single user with username `user` and password `password`.
