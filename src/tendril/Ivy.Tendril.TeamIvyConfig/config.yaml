codingAgent: claude
jobTimeout: 30
staleOutputTimeout: 10
maxConcurrentJobs: 30
projects:
- name: Framework
  color: Blue
  meta:
    slackEmoji: ':hammer_and_wrench:'
  repos:
  - &o5
    path: D:\Repos\_Ivy\Ivy-Framework
    prRule: yolo
  verifications:
  - &o0
    name: DotnetBuild
    required: true
  - &o1
    name: DotnetFormat
    required: true
  - &o2
    name: DotnetTest
    required: true
  - name: FrontendLint
    required: true
  - name: IvyFrameworkVerification
    required: true
  - name: NewWidgetChecklist
  - &o3
    name: CheckResult
    required: true
  context: >
    Ivy Framework is the core UI framework. Key folders: - src\Ivy\Widgets\ — backend widget classes - src\frontend\src\widgets\ — React widget components - src\Ivy.Docs.Shared\Docs\ — documentation - src\Ivy.Samples.Shared\ — sample apps Instructions: See AGENTS.md in repo root
  reviewActions:
  - name: Sample
    condition: Test-Path "artifacts\sample\*.csproj"
    action: dotnet run --project artifacts\sample --browse --find-available-port
  - name: Docs
    condition: Test-Path "worktrees\Ivy-Framework\src\Ivy.Docs"
    action: dotnet run --project worktrees\Ivy-Framework\src\Ivy.Docs --browse --find-available-port
  - name: Samples
    condition: Test-Path "worktrees\Ivy-Framework\src\Ivy.Samples"
    action: dotnet run --project worktrees\Ivy-Framework\src\Ivy.Samples --browse --find-available-port
  hooks:
  - name: SlackNotify
    when: after
    promptwares:
    - MakePr
    condition: ''
    action: pwsh -NoProfile -File D:\Tendril\Hooks\NotifySlack.ps1
  repoPaths:
  - D:\Repos\_Ivy\Ivy-Framework
- name: Agent
  color: Purple
  meta:
    slackEmoji: ':robot_face:'
  repos:
  - path: D:\Repos\_Ivy\Ivy-Agent
    prRule: yolo
  - &o4
    path: D:\Repos\_Ivy\Ivy
    prRule: yolo
  verifications:
  - *o0
  - *o1
  - *o2
  - *o3
  context: >
    Ivy Agent is the AI agent server. Key folders: - Ivy.Agent.Server\ — agent server - Ivy.Agent\Agents\Personas\Prompts\ — persona prompts - Ivy.Internals\Workflows\ — workflow definitions - Ivy.Agent\Tools\ — tool implementations
  reviewActions: []
  hooks:
  - name: SlackNotify
    when: after
    promptwares:
    - MakePr
    condition: ''
    action: pwsh -NoProfile -File D:\Tendril\Hooks\NotifySlack.ps1
  repoPaths:
  - D:\Repos\_Ivy\Ivy-Agent
  - D:\Repos\_Ivy\Ivy
- name: TestManager
  color: Rose
  meta:
    slackEmoji: ':test_tube:'
  repos:
  - path: D:\Repos\_Ivy\Ivy-Agent\Ivy.Agent.Test.Studio
    prRule: yolo
  - path: D:\Repos\_Ivy\Ivy-Agent\Ivy.Agent.Test.Manager
    prRule: yolo
  verifications:
  - *o0
  - *o1
  - *o2
  - *o3
  context: >
    Test Manager for Ivy Agent. Key folders: - Ivy.Agent.Test.Manager\ — CLI for managing test cases, runs, and scoring - Ivy.Agent.Test.Studio\ — TUI app for test management UI See AGENTS.md in Ivy.Agent.Test.Manager folder for CLI reference.
  reviewActions: []
  hooks:
  - name: SlackNotify
    when: after
    promptwares:
    - MakePr
    condition: ''
    action: pwsh -NoProfile -File D:\Tendril\Hooks\NotifySlack.ps1
  repoPaths:
  - D:\Repos\_Ivy\Ivy-Agent\Ivy.Agent.Test.Studio
  - D:\Repos\_Ivy\Ivy-Agent\Ivy.Agent.Test.Manager
- name: Console
  color: Teal
  meta:
    slackEmoji: ':desktop_computer:'
  repos:
  - *o4
  verifications:
  - *o0
  - *o1
  - *o2
  - *o3
  context: >
    - Ivy.Console\ — desktop CLI and TUI client - Ivy.Internals\ — shared internals - Ivy.Studio\ — vibe-coding agent
  reviewActions: []
  hooks:
  - name: SlackNotify
    when: after
    promptwares:
    - MakePr
    condition: ''
    action: pwsh -NoProfile -File D:\Tendril\Hooks\NotifySlack.ps1
  repoPaths:
  - D:\Repos\_Ivy\Ivy
- name: Tendril
  color: Emerald
  meta:
    slackEmoji: ':seedling:'
  repos:
  - *o5
  verifications:
  - *o0
  - *o1
  - name: DotnetTest
  - *o3
  context: >
    Plan management TUI. Key folders: - Services\ — ConfigService, PlanReaderService, JobService - Apps\ — PlansApp, JobsApp UI - Promptwares\ — MakePlan, UpdatePlan, SplitPlan, ExpandPlan This project uses the Ivy Framework - there's CLI helper methods you can use:  ivy docs AGENTS.md  ivy question "How do I make a button in Ivy?"
  reviewActions: []
  hooks:
  - name: SlackNotify
    when: after
    promptwares:
    - MakePr
    condition: ''
    action: pwsh -NoProfile -File D:\Tendril\Hooks\NotifySlack.ps1
  repoPaths:
  - D:\Repos\_Ivy\Ivy-Framework
- name: Mcp
  color: Sky
  meta:
    slackEmoji: ':electric_plug:'
  repos:
  - path: D:\Repos\_Ivy\Ivy-Mcp
    prRule: yolo
  verifications:
  - *o0
  - *o1
  - *o2
  - *o3
  context: >
    MCP service for IvyQuestions and IvyDocs.
  reviewActions: []
  hooks:
  - name: SlackNotify
    when: after
    promptwares:
    - MakePr
    condition: ''
    action: pwsh -NoProfile -File D:\Tendril\Hooks\NotifySlack.ps1
  repoPaths:
  - D:\Repos\_Ivy\Ivy-Mcp
- name: Services
  color: Pink
  meta:
    slackEmoji: ':cloud:'
  repos:
  - path: D:\Repos\_Ivy\Ivy-Services
    prRule: yolo
  verifications:
  - *o0
  - *o1
  - *o2
  - *o3
  - name: GoBuild
    required: true
  - name: GoVet
    required: true
  - name: GoTest
  context: >
    Ivy Services — backoffice .NET monorepo. Key folders:
    - Ivy.Admin/ — Admin web app (Ivy Framework)
    - Ivy.Billing/ — Billing API with Stripe integration
    - Ivy.Data/ — Shared EF Core database layer
    - Ivy.Services.Automation/ — Azure Functions
    - Ivy.Email/ — Email templating (Resend API)
    - ivy-proxy/ — Go reverse proxy
    Instructions: See CLAUDE.md in repo root
  reviewActions: []
  hooks:
  - name: SlackNotify
    when: after
    promptwares:
    - MakePr
    condition: ''
    action: pwsh -NoProfile -File D:\Tendril\Hooks\NotifySlack.ps1
  repoPaths:
  - D:\Repos\_Ivy\Ivy-Services
verifications:
- name: DotnetBuild
  prompt: Run `dotnet build --warnaserror` in the plan's worktree directory (not the original repo) and verify it succeeds with zero errors and zero warnings.
- name: DotnetTest
  prompt: >
    Run dotnet tests in the plan's worktree directory (not the original repo). 1. Read the plan revision's **## Tests** section for test scope instructions. 2. If the plan specifies a `dotnet test --filter` expression, run:
       `dotnet test --filter "<filter from plan>"`
       Common filter examples:
       - By class: `dotnet test --filter "FullyQualifiedName~MyTestClass"`
       - By namespace: `dotnet test --filter "Namespace~MyNamespace"`
       - Multiple: `dotnet test --filter "FullyQualifiedName~TestA|FullyQualifiedName~TestB"`
    3. If the plan says "No existing test coverage" or "No dotnet tests needed", skip with Pass. 4. If the plan says "Run all tests (broad cross-cutting change)", run `dotnet test` (all tests). 5. If the plan does NOT specify any test scope at all, FAIL the verification with message:
       "No test filter specified in plan. Plans must specify a dotnet test --filter expression in the ## Tests section."
    6. Verify all executed tests pass.
- name: DotnetFormat
  prompt: Run `dotnet format` in the plan's worktree directory (not the original repo), scoped to only the files changed by this plan's commits. Get the list of changed .cs files with `git diff --name-only --diff-filter=ACM HEAD~<N> -- '*.cs'` (where N is the number of commits in this execution), then run `dotnet format --include <files>`. If it modifies any files, stage and commit them. If no .cs files were changed, skip this verification.
- name: FrontendLint
  prompt: Run `cd src\frontend && npm run lint && npm run format:check` and verify no errors.
- name: IvyFrameworkVerification
  prompt: 'Run the IvyFrameworkVerification promptware to visually verify UI changes. Execute: `pwsh -NoProfile -File "$TENDRIL_HOME\Promptwares\IvyFrameworkVerification\IvyFrameworkVerification.ps1" -PlanPath "<PlanFolder>"`. Creates a temp project with demo apps, runs Playwright tests, and copies artifacts to <PlanFolder>\artifacts\. Write the verification report to <PlanFolder>\verification\IvyFrameworkVerification.md.'
- name: CheckResult
  prompt: >
    Verify the implementation matches what was requested in the plan. 1. Read the plan revision from `<PlanFolder>\revisions\` (latest numbered .md file) 2. Read all commits made by this execution from `plan.yaml` `commits` list 3. For each commit, review the diff (`git show <hash>`) in the worktree 4. Compare the plan's **Problem** and **Solution** sections against the actual changes:
       - Are all described changes present?
       - Are there unexpected\unrelated changes?
       - Do the changes solve the stated problem?
    5. If the plan has a **Tests** section, verify those tests were actually written 6. Write the verification report to `<PlanFolder>\verification\CheckResult.md` Report format: ``` # CheckResult

    - **Date:** <CurrentTime> - **Result:** Pass \ Fail - **Attempts:** 1

    ## Requested vs Delivered | Requirement | Status | Notes | |-------------|--------|-------| | <from plan> | Done \ Missing \ Partial | <details> |

    ## Unexpected Changes

    <list any changes not described in the plan, or "None">

    ## Output

    <summary assessment>

    ## Fixes Applied

    None

    ## Issues Found

    <any gaps between plan and implementation, or "None"> ```
- name: NewWidgetChecklist
  prompt: >
    Verify all 6 required elements exist for any new widget created by this plan: 1. Backend widget class — src\Ivy\Widgets\<WidgetName>.cs with [Prop]\[Event] attributes, [JsonIgnore] on delegates, Has* booleans 2. Frontend React component — src\frontend\src\widgets\<widgetName>\<WidgetName>Widget.tsx 3. Index export — src\frontend\src\widgets\<widgetName>\index.ts 4. Widget map registration — import + entry in src\frontend\src\widgets\widgetMap.ts 5. Sample app — src\Ivy.Samples.Shared\Apps\Widgets\<WidgetName>App.cs 6. Documentation page — src\Ivy.Docs.Shared\Docs\02_Widgets\<category>\<WidgetName>.md Check each file exists and follows conventions. Report which elements are present vs missing.
- name: GoBuild
  prompt: >
    Run `go build ./...` in the Go source directory of the project repo.
    For Services: `cd ivy-proxy && go build ./...`
    Verify the build completes with zero errors.
- name: GoTest
  prompt: >
    Run `go test ./...` in the Go source directory of the project repo.
    For Services: `cd ivy-proxy && go test ./...`
    Verify all tests pass. If no test files exist, report "No tests found" and pass.
- name: GoVet
  prompt: >
    Run `go vet ./...` in the Go source directory of the project repo.
    For Services: `cd ivy-proxy && go vet ./...`
    Verify no issues are reported.
planTemplate: >
  # {title}

  ## Problem

  ## Solution

  ## Tests

  **New tests:**

  (specify tests to write for new functionality, or "None" if no new tests needed)

  **Test scope (dotnet test --filter):**

  `FullyQualifiedName~Namespace.SpecificTestClass` (or "No existing test coverage" \ "Run all tests — <justification>")

  ## Verification
editor:
  command: code
  label: VS Code
llm:
  endpoint: DotnetUserSecrets:OpenAi:Endpoint
  apiKey: DotnetUserSecrets:OpenAi:ApiKey
  model: gpt-4o-mini
promptwares:
  MakePlan:
    profile: balanced
    allowedTools:
    - Read
    - Glob
    - Grep
    - Bash
    - Write
    - Edit
  ExecutePlan:
    profile: deep
    allowedTools:
    - Read
    - Write
    - Edit
    - Glob
    - Grep
    - Bash
  ExpandPlan:
    profile: balanced
    allowedTools:
    - Read
    - Glob
    - Grep
    - Bash
    - Write
  MakePr:
    profile: balanced
    allowedTools:
    - Read
    - Bash
    - Write
    - Edit
  UpdatePlan:
    profile: balanced
    allowedTools:
    - Read
    - Glob
    - Grep
    - Bash
    - Write
  SplitPlan:
    profile: balanced
    allowedTools:
    - Read
    - Bash
    - Write
  CreateIssue:
    profile: balanced
    allowedTools:
    - Read
    - Bash
  IvyFrameworkVerification:
    profile: balanced
    allowedTools:
    - Read
    - Write
    - Bash
codingAgents:
- name: claude
  arguments: ''
  profiles:
  - name: deep
    model: opus
    effort: max
    arguments: ''
  - name: balanced
    model: sonnet
    effort: high
    arguments: ''
  - name: quick
    model: haiku
    effort: low
    arguments: ''
- name: codex
  arguments: ''
  profiles:
  - name: deep
    model: gpt-5.4
    effort: high
    arguments: ''
  - name: balanced
    model: gpt-5.4-mini
    effort: medium
    arguments: ''
  - name: quick
    model: gpt-5.3-codex
    effort: low
    arguments: ''
- name: gemini
  arguments: ''
  profiles:
  - name: deep
    model: gemini-3-flash-preview
    effort: ''
    arguments: ''
  - name: balanced
    model: gemini-2.5-flash
    effort: ''
    arguments: ''
  - name: quick
    model: gemini-2.5-flash-lite
    effort: ''
    arguments: ''
telemetry: true
levels:
- name: Bug
  badge: Destructive
- name: Critical
  badge: Warning
- name: NiceToHave
  badge: Outline
- name: Epic
  badge: Info
